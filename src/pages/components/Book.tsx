import React from 'react';
import { motion, } from "framer-motion";

const Book = () => {

    return (
        <motion.div className={`w-full -z-10 h-96 bg-[var(--main-color)] absolute bottom-full border-t rounded-t-2xl border-[#ffffff]`}>
            <div className="relative">

            </div>
        </motion.div>

    );
};

export default Book;