export const visible:Record<string, any> = { opacity: 1, y: 0, transition: { duration: 0.7 } };
export const hidden:Record<string, any> = { opacity: 0, y: -20 };
export const viewport:Record<string, any> = { amount: 0.4, once: true};